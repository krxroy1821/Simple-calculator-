# Simple-calculator-
1. Displays a menu with 4 options:
1 - Addition
2 - Subtraction
3 - Multiplication
4 - Division


2. Takes the user’s choice and two numbers as input.


3. Uses a switch statement to perform the selected operation.


4. Checks for division by zero using the ternary operator (b != 0 ? a / b : 0).


5. Prints the result up to 2 decimal places.

